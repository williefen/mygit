<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">


    <!-- 真正可以产生Connection的ConnectionFactory，由对应的 JMS服务厂商提供 -->
    <bean id="targetConnectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
        <property name="brokerURL" value="tcp://192.168.12.168:61616"/>
    </bean>

    <!-- Spring用于管理真正的ConnectionFactory的ConnectionFactory -->
    <bean id="connectionFactory" class="org.springframework.jms.connection.SingleConnectionFactory">
        <!-- 目标ConnectionFactory对应真实的可以产生JMS Connection的ConnectionFactory -->
        <property name="targetConnectionFactory" ref="targetConnectionFactory"/>
    </bean>

    <!-- Spring提供的JMS工具类，它可以进行消息发送、接收等 -->
    <bean id="jmsTemplate" class="org.springframework.jms.core.JmsTemplate">
        <!-- 这个connectionFactory对应的是我们定义的Spring提供的那个ConnectionFactory对象 -->
        <property name="connectionFactory" ref="connectionFactory"/>
    </bean>

    <!--商品审核之后的搜索系统的消息队列 -->
    <bean id="solrItemQueue" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg value="pinyougou_solr_item_queue"/>
    </bean>

    <!--商品删除之后的搜索系统的消息队列 -->
    <bean id="solrItemDeleteQueue" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg value="pinyougou_solr_item_delete_queue"/>
    </bean>

    <!-- messageListener实现类 -->
    <bean id="solrItemMessageListener" class="com.pinyougou.search.activemq.listener.SolrItemQueueMessageListener"/>

    <!-- 配置一个jms监听容器 -->
    <bean id="jmsContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory"/>
        <property name="destination" ref="solrItemQueue"/>
        <property name="messageListener" ref="solrItemMessageListener"/>
    </bean>

    <!-- messageListener实现类 -->
    <bean id="solrItemDeleteMessageListener" class="com.pinyougou.search.activemq.listener.SolrItemDeleteMessageListener"/>

    <!-- 配置一个jms监听容器 -->
    <bean id="jmsContainer2" class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory"/>
        <property name="destination" ref="solrItemDeleteQueue"/>
        <property name="messageListener" ref="solrItemDeleteMessageListener"/>
    </bean>

</beans>